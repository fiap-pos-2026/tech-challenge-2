apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tech-challenge

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/name: tech-challenge-core
      app.kubernetes.io/part-of: tech-challenge

resources:
  - deployment.yaml
  - service.yaml
